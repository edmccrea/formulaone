// import type { PageServerLoad } from "./$types";

// export const load = (async () => {
//   const raceRes = await prisma.races.findMany();
//   const races = raceRes.sort((a, b) => {
//     return a.calendar_round - b.calendar_round;
//   });

//   return {
//     races,
//   };
// }) satisfies PageServerLoad;
